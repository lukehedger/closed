name: Opened

on:
  pull_request:
    branches:
    - master
    types: [opened]

jobs:
  opened:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2

    - name: Opened?
      run: echo Opened
